<%= form_for(@time_off_entry) do |f| %>
  <% if @time_off_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_off_entry.errors.count, "error") %> prohibited this time_off_entry from being saved:</h2>

      <ul>
      <% @time_off_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_date %>
    <%= f.date_select :entry_date %>
  </div>
  <div class="field">
    <%= f.label :time_off_type %>
    <%= f.select :time_off_type, options_for_select(TimeOffEntry::VALID_TYPES) %>
  </div>
  <div class="field">
    <%= f.label :amount %>
    <%= f.select :amount, options_for_select(TimeOffEntry::VALID_AMOUNTS) %>
  </div>
  <div class="field">
    <%= f.label :notes %>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
