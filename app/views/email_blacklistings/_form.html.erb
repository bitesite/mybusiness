<%= form_for(@email_blacklisting) do |f| %>
  <% if @email_blacklisting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_blacklisting.errors.count, "error") %> prohibited this email_blacklisting from being saved:</h2>

      <ul>
      <% @email_blacklisting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
