<%= form_for(@video_listing, :html => {:multipart => true}) do |f| %>
  <% if @video_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_listing.errors.count, "error") %> prohibited this video_listing from being saved:</h2>

      <ul>
      <% @video_listing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
